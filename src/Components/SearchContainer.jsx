// / Create framework
// create states that uses props
// direct called API
// create the container
// Define what is in container
// export back to APP

import React from "react";

function SearchContainer() {
  return <div></div>;
}

export default SearchContainer;
